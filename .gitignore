# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# ────────────── Dependencias / builds
/node_modules
/.next/
out/
build/

# ────────────── Cachés / debug
.pnp.js
/.pnp
.yarn/install-state.gz
.eslintcache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ────────────── Tests y coverage
coverage/

# ────────────── Producción/hosting
.vercel

# ────────────── Archivos del SO
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# ────────────── Editores/IDE
.vscode/
.idea/
*.swp
*.swo

# ────────────── Keys y varios
*.pem

# ────────────── Env (no subir secretos)
.env
.env.*
!.env.example
# (tu proyecto ya usa .env.local; igual queda cubierto)
# Si prefieres permitir .env.production, quita la línea .env.*

# ────────────── TypeScript (si lo usas)
/next-env.d.ts
*.tsbuildinfo

# ────────────── Gestores de paquetes (estandariza en npm)
yarn.lock
pnpm-lock.yaml
# (mantenemos package-lock.json en el repo)
